# commit-msg-helper
A pre-commit helper for commit messages

This repository contains the installable Python package `commit-msg-helper`.
It currently exposes two no-op console scripts:

- `branch-needs-jira`
- `message-needs-jira`
